#include <stdio.h>
#include <sys/types.h>
#include <unistd.h>

int main () {
    int i = 0, total=100000000, wait=200000, ini=0, pid, fim=total;
    
    while (ini<fim) {
	    for (int j=0; j++; j<wait);
	    ini++;
    }
    printf("end\n");

    return 0;
} 
