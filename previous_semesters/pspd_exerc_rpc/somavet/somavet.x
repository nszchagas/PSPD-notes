/* Arquivo de definicao para um parametro de tamanho variavel */

typedef int vetint<>; /* declaracao de um vetor de inteiros */

program SOMAVET_PROG {
   version SOMAVET_VERSION {
     int SOMAV(vetint) = 1;
   } = 1;
} = 0x88888;
